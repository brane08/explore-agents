<div class="error" data-code="PATTERN_UNRECOGNIZED">
  <strong>PATTERN_UNRECOGNIZED</strong> — No agent, template, or tool coverage matches this task.
  <p>The normalized residual below is recorded as B2 backlog / template-gap signal.</p>
  {% if error.context %}<p><code>{{ error.context }}</code></p>{% endif %}
</div>
