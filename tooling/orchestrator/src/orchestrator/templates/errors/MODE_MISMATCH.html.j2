<div class="error" data-code="MODE_MISMATCH">
  <strong>MODE_MISMATCH</strong> — Playbook mode precondition failed (invocation bug).
  {% if error.context %}<p><code>{{ error.context }}</code></p>{% endif %}
</div>
