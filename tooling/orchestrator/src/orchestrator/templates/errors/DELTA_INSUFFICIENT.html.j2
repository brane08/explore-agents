<div class="error" data-code="DELTA_INSUFFICIENT">
  <strong>DELTA_INSUFFICIENT</strong> — The residual gap is not component-shaped.
  <p>Escalation is an explicit orchestrator decision — nothing happens automatically.</p>
  {% if error.context %}<p><code>{{ error.context }}</code></p>{% endif %}
</div>
